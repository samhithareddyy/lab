#include <stdio.h>
int main(){
	float seconds, mins, hours;
	printf("enter the value of mins");
	scanf("%f", &mins);
	printf("enter the value of hours");
	scanf("%f", &hours);
	printf("the no.of seconds is %f", mins*60);
		printf("the no.of seconds is %f", hours*3600);
}
